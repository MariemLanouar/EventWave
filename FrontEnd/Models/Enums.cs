namespace FrontEnd.Models
{
    public enum EventStatus
    {
        Draft,
        Published,
        Cancelled
    }

    public enum TicketType
    {
        VIP,
        Regular,
        Premium
    }

    public enum PaymentMethod
    {
        CreditCard,
        DebitCard,
        MobileWallet
    }
}
